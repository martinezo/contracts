.modal-header
  %h3.modal-title
    = t('delete')

.modal-body
  %h4.modal-title
    = t('Are you sure?')

.modal-footer
  = link_to t('yes'), {:controller => :devices, :action => 'destroy',:id => @id}, :method => :delete, class: "btn btn-sm btn-tss"
  = link_to t('cancel'), {:controller => :devices, :action => 'index' }, :method => :get, class: "btn btn-sm btn-tss"
~                                                                                                                               
